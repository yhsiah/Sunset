module.exports = {
// use this to store variables or other data that may be useful for testing
// to use these, use `browser.globals.testData()` and then further specify the data required - e.g. login.username

  login: {
    username: 'example@example.com',
    password: 'Password'
  }

  testData: {
    something: 'something',
    somethingElse: 'something else'
  }
}
